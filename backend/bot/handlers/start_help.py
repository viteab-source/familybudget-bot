"""
bot/handlers/start_help.py — Команды /start и /help
"""

from aiogram import types
from aiogram.filters import CommandStart, Command

from bot.config import logger


async def cmd_start(message: types.Message):
    """Обработчик команды /start"""
    logger.info(f"User {message.from_user.id} started bot")
    
    welcome_text = """
👋 Добро пожаловать в FamilyBudget Bot!

Это бот для совместного учёта семейных расходов. Ты можешь:

• 💰 Записывать расходы и доходы
• 📊 Смотреть отчёты по категориям
• 👨‍👩‍👧 Приглашать членов семьи
• 📱 Использовать голос для быстрого ввода

Команди:
/help — полный список команд
/me — информация о тебе
/add — добавить расход
/income — добавить доход
/report — отчёт за 14 дней
"""
    
    await message.answer(welcome_text)


async def cmd_help(message: types.Message):
    """Обработчик команды /help"""
    help_text = """
📖 **КОМАНДЫ БОТА**

**Личные:**
/me — мой профиль
/setname *имя* — установить имя
/family — информация о семье

**Семья:**
/family_invite — получить код приглашения
/family_join *код* — присоединиться к семье
/family_rename *имя* — переименовать семью
/family_leave — выйти из семьи

**Транзакции:**
/add *сумма* *категория* — добавить расход
/income *сумма* *категория* — добавить доход
/aiadd — записать расход через ИИ
/last — последняя транзакция
/edit_last *новая_сумма* — изменить последнюю
/del_last — удалить последнюю

**Отчёты:**
/report — сводка (последние 14 дней)
/report_balance — баланс по категориям
/report_members — расходы по членам семьи
/report_shops — расходы по магазинам
/report_export — экспорт в CSV

**Категории:**
/categories — список категорий
/cat_add *название* — добавить категорию
/cat_delete *название* — удалить категорию
/cat_rename *старое* *новое* — переименовать
/cat_merge *откуда* *куда* — слить категории

**Напоминания:**
/reminders — список напоминаний
/remind_add *описание* — добавить напоминание

**Бюджеты:**
/budget_set *категория* *сумма* — установить бюджет
/budget_remove *категория* — удалить бюджет
/budget_list — список бюджетов
"""
    
    await message.answer(help_text, parse_mode="Markdown")
